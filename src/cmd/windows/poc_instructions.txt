build binary
    go build -o launcher.exe

launch binary
    launcher -daemon=true -shell=powershell -path=test.ps1
        or
    launcher -daemon=true -shell=cmd -path=test.bat

Trying to kill new window with Ctrl-C should fail
Attempt to kill the process tree for the console that launched the command should not affect the launched script
